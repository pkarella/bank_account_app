%h1
  Bank Accounts

%table.table.table-bordered
  %thead
    %tr
      %th Account Number
      %th Balance
      %th Actions
  %tbody
    - @bank_accounts.each do |accounts |
      %tr
        %td #{accounts.account_number}
        %td #{number_to_currency(accounts.balance, unit: '$')}
        %td
          = link_to bank_account_path(accounts.id), class: 'btn btn-primary' do
            Show
